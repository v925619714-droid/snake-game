@AGENTS.md

## 📋 Дашборд задач (общий, в корне «Вайбкодинг»)

Единый дашборд задач/проектов/вопросов — `..\ДАШБОРД.html`. **В любой сессии веди его сам, без напоминаний и без спроса:** заводи задачи и проставляй статусы (задачи `todo`→`in_progress`→`done`/`blocked`; вопросы 🔴`wait`→🟡`default`→🟢`done`/⏸️`later`), обновляй поле `updated`. Правь ТОЛЬКО JSON-блок `#registry`, потом проверяй валидность JSON. Полные правила — `..\CLAUDE.md` и память `feedback-maintain-dashboard` / `tasks-dashboard`.
